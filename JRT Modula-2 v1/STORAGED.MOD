definition module storage;

export qualified allocate, deallocate;

procedure allocate (var a : integer; s : integer);
procedure deallocate (var a : integer; s : integer);

end storage.

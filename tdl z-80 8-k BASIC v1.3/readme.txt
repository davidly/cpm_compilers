sbasic can't load() text files; it only loads the binary form of parsed files
previously created with save().

instead, redirect stdin.

sbasic has no means (that I've found) to return to the OS aside from ^x


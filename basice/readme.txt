runbad.com is a version found with all other files. It's clearly corrupted
in the range 0x1000-0x10ff (with pip commands then ^z characters). It
otherwise appears identical to run.com aside from a single byte at offset
0x2a22 the last byte at offset 0x2dff.







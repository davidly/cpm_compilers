$nolist
/*
    FCB Literals
*/

  declare fcb$descriptor literally
    'structure (
       et byte,
       fn (8) byte,
       ft (3) byte,
       ex byte,
       nu address,
       rc byte,
       dm (16) byte,
       nr byte,
       r0r1 address,
       r2 byte )';

$list
